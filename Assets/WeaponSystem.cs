using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponSystem : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    /*
    void FireLaser(Vector3 target)
    {
        print("Pew!");
        Ray ray;
        RaycastHit hit;
        ray = FPVcam.ScreenPointToRay(Input.mousePosition);
        GameObject bolt = Instantiate(plasmaBoltPreFab, weaponPoint01.position, Quaternion.Euler(90, 0, 0), weaponPoint01);
        GameObject bolt2 = Instantiate(plasmaBoltPreFab, weaponPoint02.position, Quaternion.Euler(90, 0, 0), weaponPoint02);


    }
    */
}
